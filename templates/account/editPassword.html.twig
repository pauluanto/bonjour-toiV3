{% extends 'base.html.twig' %}

{% block title %}Modifier mon compte{% endblock %}

{% block monTitre %}Modifier {% endblock %}
{% block body %}
    {{form_start(form)}}

    {{form_label(form.username, 'Nouveau pseudo')}}
    {{form_widget(form.username)}}


    {{form_label(form.plainPassword, 'Mot de passe actuel')}}
    {{form_widget(form.plainPassword)}}




    <input type="submit" class="btn btn-primary" value="valider">
    <a href="{{ 'account_reset_password' }}" class="btn btn-primary">Modifier mon mot de passe</a>
    {{form_end(form)}}

{% endblock %}